# Handoff — Architecture Refactor Session (2026-08-21)

This is a working handoff for the next session continuing the VoidFall Unity
architecture refactor. Read it fully before changing anything.

## Session 2 addendum (same day) — blocker resolved, gates green

- `cb800ac` completed the roulette feature and resolved this handoff's
  recorded blocker: the full `RouletteRules` API now exists; the test
  asmdef compile break is gone.
- `3ddfa84` added the missing `VoidFall.Content` reference to the EditMode
  test asmdef (playbook pitfall: Content types live in the Core namespace).
- `f0819d1` migrated the meteor advance loop's state half into
  `GameSim.AdvanceMeteors` (player kinematics still by-ref from the runtime;
  detonation damage resolution intentionally left on the runtime).
- Verified after all three commits: rebuild 0 errors / 14 known warnings,
  **EditMode 65/65** (57 prior + 8 new roulette tests), **PlayMode golden
  master passed against pinned hash `15261090775683682834`**.
- Remaining work list below is unchanged except item 1 is done and the meteor
  family's advance half is complete (spawn + detonation halves remain).

---

## Ground rules established this session

1. **Two agents work this repo concurrently** — one on game content (roulette
   feature in `Content/` + `UI/Views/RouletteView.cs`), one on structure.
   **Stage commits by explicit file path, never `git add -A`.**
2. The PlayMode golden-master test is the behavior contract. Structural
   refactors must leave its pinned hash unchanged:
   `15261090775683682834`. Intentional gameplay changes regenerate the hash
   in a separate commit with an explanation.
3. Content agents and structure agents stay out of each other's files.

## Verification loop

```text
dotnet build VoidFall.Runtime.csproj -t:Rebuild        # 0 errors, 14 known CS0649 warnings
Unity -batchmode -runTests -testPlatform EditMode      # expect all green
Unity -batchmode -runTests -testPlatform PlayMode
  -testFilter "VoidFall.Tests.PlayMode.SimulationGoldenMasterTests"
```

Full commands and every pitfall learned so far are documented in
`Docs/RefactoringPlaybook.md` — read it before scripting any file surgery.

## What was completed (all committed, branch `audit/save-fixes-and-render-cleanup`)

| Commit | Work |
| --- | --- |
| `63b6ce5` | Rescued untracked WIP: uGUI layer, prebaked assets, editor tests |
| `0aabfb6` | Monolith split into partial class files by concern |
| `adffb12` | `InputReader` service extracted (all device polling) |
| `bdd3376` | Golden-master PlayMode harness created and pinned |
| `493f275` | Six identical slot-order trios unified into `SlotOrder` |
| `0325ea3` | VF-002/VF-006 UI layout fixes, verified with player captures |
| `d934413` | VF-009 HUD per-frame allocation fix |
| `a470c54` | All HUD code consolidated into `VoidFallGameRuntime.Hud.cs` |
| `d97d2c3`+`6cbef3f`+`552e80e` | **FxSim complete**: cosmetic-FX state + update loops + spawn insertion out of the runtime; views stay behind |
| `c8914bd` | **GameSim v0**: all combat state (enemies/bullets/shots/pickups/bosses/meteors arrays, orders, grid buffers, combat RNG; 543 refs) moved into `GameSim`; types promoted to `CombatStateTypes.cs` |
| `b3303ea` | GameSim v1 batch one: order bookkeeping + grid rebuild methods moved inward |
| `62f620b` | GameSim v1 batch two: separation pass, effect snapshots, Active* counters, FindInactive overloads moved inward |

Docs: `Docs/Architecture.md`, `Docs/RefactoringPlaybook.md`,
`MIGRATION_STATUS.md` addendum.

## Current state at handoff

- Working tree contains the OTHER agent's roulette feature, mid-flight:
  `Content/RouletteRules.cs`, `UI/Core/UIManager.cs` (modified),
  `UI/Views/RouletteView.cs`, `Tests/Editor/RouletteRulesTests.cs` (new).
- **Known blocker**: `RouletteRulesTests.cs` references API that does not yet
  exist (`RouletteRules.DefaultTable()`, `RouletteSession`, `RouletteTier`) —
  the rules file has `RoulettePrizeKind` but not the rest. This breaks the
  EditMode test asmdef compile, which blocks ALL Unity test runs including the
  golden master. Resolve it with the content agent's work, not by deleting.
- My batch two (`62f620b`) passed the compile gate but its golden-master run
  was blocked by the above. **First task of next session: fix/complete the
  roulette test file situation, then re-run the golden master and confirm the
  pinned hash before any further refactoring.**

## What remains (in order)

1. Re-run golden master after the roulette file compiles; confirm hash.
2. **GameSim v1 continues** — migrate pure-state method bodies family by
   family into `GameSim.cs` using the playbook recipe (state half moves,
   view-sync half stays as same-named wrapper):
   - meteor spawn/update/detonation state halves (detonation damage
     resolution couples to player/enemies — split carefully);
   - bullet and hostile-shot advance loops (large; expired-slot pattern);
   - pickup update/spawn (magnet logic reads player position, which still
     lives on the runtime — either pass it as a parameter or migrate player
     kinematic state into GameSim first);
   - enemy AI last (largest, most entangled).
3. Player kinematics (`_playerPosition`, `_playerVelocity`, health, timers)
   move into GameSim after the families that read them are handled.
4. Promote `HudPresenter` from the consolidated `.Hud.cs` partial into a real
   class; same for `ArenaRenderer`.
5. Move menu controller logic into `VoidFall.UI`; shrink the runtime
   MonoBehaviour to a composition root (bootstrap + wiring + Update pump).
6. Then the stale-doc cleanup and release-hygiene items VF-010..VF-012 in
   `Docs/AI/UnityProjectHealth.md`.

## Review request

The requesting owner will have this session's work reviewed after the next
session runs. Review checklist: golden-master hash still pinned value across
all new commits; no commits contain both content and structural changes;
`git log --stat` shows explicit-path staging only.
