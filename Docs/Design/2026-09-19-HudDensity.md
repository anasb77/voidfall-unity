# Approved HUD and density pass

Owner approved browser study `../Prototypes/hud-v2`, reduced by 40%, then increased by 10% relative to that size: final scale 0.66. The main runtime uses bundled Chakra Petch, green XP, rose-to-orange HP with current/max, centered timer/level/pressure and stacked score/kills/Scraps. Bottom arsenal has four live slots plus the unlockable fifth; legendary slot is separate. Passive and late cards share a compact bottom-right grid with actual names, ranks and hover descriptions. Second Wind displays remaining cooldown. Existing boss, Overclock, modal and transition ownership stays intact.

Director I v4 doubles ambient arrival frequency and early population targets, shortens ordinary encounter gaps from 14–28s to 7–14s, retains the 750 total cap and attack admission safeguards. Full circle swarms use the V1 count/radius and 34s cadence, delayed by existing boss/recovery/introduction safety gates; tier-I purple runners plus introduced green swarmers. Runner tier-I silhouette is the V1 diamond.

Pressure starts at 1.00x and progresses to existing director ceilings. A separate zero-based progression quantity keeps existing enemy stat/attack ramp timing; starting pressure does not grant free progression credit.

Spiky base radius 19.5 (50% above 13); alternating one-second phases at 1x / 3x with 120ms easing, identity-staggered. Enlarged radius is 58.5. Existing bounded player-safe death chains remain. Shuriken spins at 14 rad/s, twice the prior 7.

Validation: 26 EditMode rule tests passed (`Logs/hud-density-edit.xml`); 61 PlayMode tests passed (`Logs/hud-density-play.xml`), including the unchanged golden hashes and the 32-seed sweep. Four focused HUD/runtime tests passed again after native-capture corrections (`Logs/hud-final-play.xml`). Canonical Windows build succeeded at 2026-09-19 20:21:55 +01:00, GUID 4d9515e8cada43aca0a813623158e624 (`Logs/hud-final-build.log`). Native 1280x720 captures were visually inspected for full inventory, level badge, compact metrics, low HP, cooldown and tooltip. Capture output: `C:/Users/AB/.codex/visualizations/2026/09/19/01a0b9b9-c6e3-7cc3-9856-b08efaa53c68/hud-pressure-pass/native-final/`. The isolated probe completed through Junction with no logged exceptions. The previously observed ComputeBuffer disposal warning still appears at shutdown; its owner is not established by this pass. Display preferences were restored after capture. No claims about wishlist conversion or subjective difficulty can be established by automated tests.
