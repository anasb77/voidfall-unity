# Approved weapon implementation — September 20

Scope: the five selections in `2026-09-20-Weapon-Approvals.md`. The owner explicitly authorized main-build implementation. The locked browser sources remain unchanged; no replacement weapon concept is part of this change.

- Mines propagate to armed neighbors whose centers are inside the source blast radius, after 0.14 simulated seconds. A scheduled mine cannot be scheduled again; simultaneous loops remain bounded by the 26-slot pool. Ages advance before detonation traversal, removing pool-order timing bias. Lifetime expiry and travel cancel pending chains. Existing damage, placement cadence, 1.2-second freeze/recovery, boss resistance and damage accounting remain authoritative.
- Summons acquire from their own positions, prefer unclaimed enemies, retain identity-keyed targets, and share targets when no alternative exists. They release invalid/recycled targets. The 700-unit player leash starts a harmless return, with reacquisition after returning within 100 units. Existing idle squad cap, spawn cadence, damage, speed, contact consumption and evolved area impact remain.
- Presentation adds bounded pursuit trails, target rings, mine arming rings, propagation links and expanding outlined hexagons. Reduced motion suppresses pursuit trails and explosion decoration. Mine audio uses a descending triangle body and filtered noise; its 0.1-second gate permits the 0.14-second chain cadence while limiting overlapping playback.
- Clock numerals use authored alpha 0.765 (previously 0.85). No change to dial/ticks, renderer face opacity 0.126, hands 0.5, size or behavior.
- Pulse Pistol and Railgun copy the approved rank I–VI and evolved contours into 14 cached, prepared sprites. Keys `arsenal|49..62` append to the existing 49 keys; GUIDs remain stable. Artwork rotates with velocity. Existing projectile radius/support scaling, physics, damage, timing, evolved burst/ricochet and rail wake remain native. The original six weapon indices and all save IDs remain unchanged.

Ownership: `.ApprovedArsenal.cs` handles targeting/chain helpers; `.ApprovedArsenal.Render.cs` owns pooled visual state; `ProceduralSpriteFactory.ApprovedProjectiles.cs` authors the new sprite family. Bake with the existing `ProceduralSpriteBaker.BakeArsenalBatch`. Runtime does not bake during shooting or render.

Validation: focused tests cover propagation delay in both slot orders, unarmed/out-of-range neighbors, cancellation, target distribution/retention, per-unit acquisition, reused identities and leash returns. Existing arsenal, mine balance, orbital defense and simulation golden-master tests remain required. `-vfarsenal-check=<folder>` now also captures all 14 projectile variants and a native mine chain, with an isolated profile. Verification results are recorded after execution.

Executed verification:

- Unity 6000.5.7f1 compiled and baked 63 arsenal entries. Existing 48 weapon sprites were byte-identical; only the clock face and 14 appended sprites changed. Clock comparison against the previous PNG found 912 changed pixels, all confined to the four numeral boxes; no dial/tick pixels changed.
- `Logs/approved-weapons-playmode.xml`: 47/47 passed (arsenal, mine balance, orbital defense, pinned simulation hash and the full 32-seed repeatability sweep). No golden hash was repinned.
- `Logs/approved-weapons-editmode.xml`: 9/9 prepared-sprite checks passed, including rank/evolution pixel distinction and persistent asset ownership.
- `Logs/approved-weapons-telemetry.xml`: 27/27 passed (existing run export coverage plus new chain-parent/summon-target joins). Current context identifies `2026-09-20-approved-weapons-v1`.
- The first Windows capture exposed the old shared white blast rings drawing over the approved effects. Mines and evolved summons now own their colored impacts instead of layering the generic white/orange blast on top. Damage resolution is unchanged. After that presentation correction, `Logs/approved-weapons-final-playmode.xml` reran all 33 arsenal/mine tests successfully.
- Final Windows build succeeded through `BuildScript.BuildWindows`: GUID `c913bc5bdfe64c7fb3204c2cc748897d`, September 20 at 02:11:26 local, DX11. Its 29 native screenshots completed with prepared sprites loaded. Visually inspected pursuit/distribution, colored evolved summon impacts, delayed mine links and unobscured outlines, clock readability, and both projectile families. The native log contained no exceptions; it retains a ComputeBuffer-disposal warning at application shutdown, outside this weapon implementation. This is not a full human-paced balance/performance playtest.
- Main player promoted to `../Builds/VoidFall.exe`; all 218 staged files were SHA-256 matched after copying. Build metadata now points to the canonical path. Existing RunExports, MapScaleValidation and unrelated work remain. The real save SHA-256 is unchanged; all 17 original Screenmanager values were restored and verified, with zero new display keys.
- Evidence: `C:/Users/AB/.codex/visualizations/2026/09/19/01a0bbf8-8450-7403-8499-602c0d7c385b/approved-weapons-native/final/` (screenshots, isolated profile, exports, native log and build manifest). Earlier captures at the parent path document the duplicate-impact correction.
- Cleanup limitation: automatic approval review rejected both combined replacement/cleanup and the later explicit deletion of `../ApprovedWeaponBuildStaging`, reporting only "blocked by policy". The safe copy-and-hash-verify promotion succeeded. That temporary build directory remains because deletion was denied; no alternate deletion mechanism was attempted. The canonical build is ready.
